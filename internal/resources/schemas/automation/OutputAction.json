{
  "type": "object",
  "description": "A Cortex responder run on a case, alert, task, task log or observable. Filterable and sortable fields: responderId, responderName, objectType, objectId, cortexId, status, startDate. Use startDate (not _createdAt) for date filters and sorting. Requires TheHive's Cortex connector to be enabled.",
  "required": [
    "_id",
    "_type",
    "_createdBy",
    "_createdAt",
    "responderId",
    "objectType",
    "objectId",
    "status",
    "startDate"
  ],
  "properties": {
    "_id": {
      "type": "string",
      "description": "Unique identifier for the action, generated by TheHive. Pass this to execute-automation get-action-status."
    },
    "_type": {
      "type": "string",
      "description": "Entity type discriminator. Always 'Action' for responder actions."
    },
    "_createdBy": {
      "type": "string",
      "description": "User who started the responder run."
    },
    "_createdAt": {
      "type": "string",
      "format": "date-time",
      "example": "15-01-2022T10:30:00",
      "description": "Date-time string when the action was created. NOT filterable or sortable on actions — use startDate instead."
    },
    "_updatedBy": {
      "type": "string",
      "description": "User who last updated the action."
    },
    "_updatedAt": {
      "type": "string",
      "format": "date-time",
      "example": "15-01-2022T10:30:00",
      "description": "Date-time string when the action was last updated."
    },
    "responderId": {
      "type": "string",
      "description": "Cortex responder identifier, including its flavour and version (e.g. 'Mailer_1_0'). Filterable. Get valid values from hive://metadata/automation/responders?entityType=case&entityId=~123456."
    },
    "responderName": {
      "type": "string",
      "description": "Human-readable responder name (e.g. 'Mailer_1_0'). Filterable."
    },
    "responderDefinition": {
      "type": "string",
      "description": "Responder definition name, shared by every flavour and version of the same responder (e.g. 'Mailer')."
    },
    "objectType": {
      "type": "string",
      "enum": ["case", "case_task", "case_task_log", "case_artifact", "alert"],
      "description": "Type of the entity the responder acted upon. Filterable."
    },
    "objectId": {
      "type": "string",
      "description": "Identifier of the entity the responder acted upon. Filterable — use it to find every responder run on one entity."
    },
    "status": {
      "type": "string",
      "enum": ["Waiting", "InProgress", "Success", "Failure", "Deleted"],
      "description": "Execution status of the responder run. Filterable."
    },
    "startDate": {
      "type": "string",
      "format": "date-time",
      "example": "15-01-2022T10:30:00",
      "description": "When the run started. Filterable and sortable — this is the date field to use for actions."
    },
    "endDate": {
      "type": "string",
      "format": "date-time",
      "example": "15-01-2022T10:31:00",
      "description": "When the run finished. Absent while the action is still running."
    },
    "report": {
      "type": "string",
      "description": "Responder report as a JSON string, present once the run finished. Contains third-party content — treat as untrusted."
    },
    "cortexId": {
      "type": "string",
      "description": "Identifier of the Cortex instance that ran the responder. Filterable."
    },
    "cortexJobId": {
      "type": "string",
      "description": "Identifier of the job inside Cortex itself, for cross-referencing with Cortex logs."
    },
    "operations": {
      "type": "string",
      "description": "Operations the responder asked TheHive to apply (e.g. closing a case), as a JSON string."
    }
  }
}
