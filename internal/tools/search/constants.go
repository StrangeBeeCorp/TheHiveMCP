package search

// Entity field names reused across the search tool.
const (
	fieldID    = "_id"
	fieldTitle = "title"
	// fieldCount is the key a count query's single result row carries.
	fieldCount = "_count"
)
